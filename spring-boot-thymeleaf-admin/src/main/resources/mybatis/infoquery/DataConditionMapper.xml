<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zyx.mapper.infoquery.DataConditionMapper">

	<!--表名 -->
	<sql id="tableName">
		"BON_DATA_CONDITION_TBL"
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		SHOP_CODE,
		CONDITION_CODE,
		CONDITION,
		CONDITION_YMD,
		CONDITION_VAL,
		ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{SHOP_CODE},
		#{CONDITION_CODE},
		#{CONDITION},
		#{CONDITION_YMD},
		#{CONDITION_VAL},
		to_char(sysdate,'yyyyMMdd')||POINTS_DATA_SEQ.NEXTVAL		
	</sql>
	
	<select id="list" parameterType="java.util.Map" resultType="java.util.Map">
		select 
				SHOP_CODE,
				CONDITION_CODE,
				(select name
		          from jt_dictionaries
		         where parent_id in
		               (select dic_id
		                  from jt_column_dic dic
		                 where dic.table_name = 'BON_DATA_CONDITION_TBL'
		                   and dic.table_column = 'CONDITION_CODE')
		           and bianma = CONDITION_CODE) CONDITION_CODE_DESC,
				CONDITION,
				CONDITION_YMD,
				CONDITION_VAL,
		ID
		from 
			<include refid="tableName"></include>
			where 1=1 
		
		
	</select>
	
	<select id="isExists" parameterType="java.util.Map" resultType="java.lang.Integer">
		select count(1) from 
		<include refid="tableName"></include> 
		where ID = #{ID}
	</select>
	
	<select id="listById" parameterType="java.util.Map" resultType="java.util.Map">
		select 
				SHOP_CODE,
				CONDITION_CODE,
				(select name
		          from jt_dictionaries
		         where parent_id in
		               (select dic_id
		                  from jt_column_dic dic
		                 where dic.table_name = 'BON_DATA_CONDITION_TBL'
		                   and dic.table_column = 'CONDITION_CODE')
		           and bianma = CONDITION_CODE) CONDITION_CODE_DESC,
				CONDITION,
				CONDITION_YMD,
				CONDITION_VAL,
		ID
		from 
		<include refid="tableName"></include>
		where ID = #{ID}
	</select>
	
	<insert id="save" parameterType="java.util.Map">
		insert into 
		<include refid="tableName"></include>
		(
		<include refid="Field"></include>
		) values (
		<include refid="FieldValue"></include>
		)
	</insert>
	
	<update id="update" parameterType="java.util.Map">
		update 
		<include refid="tableName"></include>
		set 
		SHOP_CODE = #{SHOP_CODE},
		CONDITION_CODE = #{CONDITION_CODE},
		CONDITION = #{CONDITION},
		CONDITION_YMD = #{CONDITION_YMD},
		CONDITION_VAL = #{CONDITION_VAL},
		ID = ID
		where ID = #{ID}
	</update>
	
	<delete id="delete" parameterType="java.util.Map">
		delete from 
		<include refid="tableName"></include>
		where ID = #{ID}
	</delete>
    
</mapper>